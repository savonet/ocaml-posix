(env
 (dev
  (flags
   (:standard -warn-error -A))))

(library
 (name posix_stat_stubs)
 (public_name posix-stat.stubs)
 (libraries posix-stat.types posix-types ctypes.stubs))

(rule
 (targets posix_stat_generated_types.ml)
 (action
  (with-stdout-to
   %{targets}
   (run ../generator/gen_types_c))))
